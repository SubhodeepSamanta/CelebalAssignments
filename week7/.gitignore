# Python
__pycache__/
*.py[cod]
.venv/
venv/
env/
.ipynb_checkpoints/

# OS / editor
.DS_Store
Thumbs.db
.idea/
.vscode/

# Spark / Delta local artefacts
spark-warehouse/
metastore_db/
derby.log
*.crc
.spark-staging*

# Regenerated by running the notebook:
#   jupyter nbconvert --to notebook --execute --inplace notebooks/delta_scd_assignment.ipynb
# The Delta tables and exports are committed here so the submission is inspectable
# without running anything. Uncomment if you would rather keep the repo lean.
# output/
# output/delta_lake/
